#include <bits/stdc++.h>
using namespace std;
#define FASTIO cin.tie(0), cout.tie(0), ios_base::sync_with_stdio()
#define endl '\n'

int main() {
    FASTIO;
    int T;
    cin >> T;
    while (T--) {
        int A, B;
        cin >> A >> B;
        cout << "yes" << endl;
    }
};