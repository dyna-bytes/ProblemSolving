#include <bits/stdc++.h>
#define FASTIO cin.tie(0), cout.tie(0), ios_base::sync_with_stdio(0)
#define endl '\n'

using namespace std;

u_int64_t N;
u_int64_t max_ans = 1000000000;

int main() {
    FASTIO;
    cin >> N;
    cout << max_ans << endl;
    return 0;
}