#include <bits/stdc++.h>

using namespace std;
#define FASTIO cin.tie(0), cout.tie(0), ios_base::sync_with_stdio(0)
#define endl '\n'

int main() {
    FASTIO;
    int N;
    cin >> N;

    if (N == 1) cout << "B" << endl;
    else cout << "A" << endl;
    return 0;
}
