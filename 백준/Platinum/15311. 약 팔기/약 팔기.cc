#include <bits/stdc++.h>
using namespace std;
#define FASTIO cin.tie(0), cout.tie(0), ios_base::sync_with_stdio(0)
#define endl '\n'

int main() {
    int K;
    cin >> K;


    cout << 2000 << endl;
    for (int i = 0; i < 1000; i++)
        cout << 1000 << ' ';
    
    for (int i = 0; i < 1000; i++)
        cout << 1 << ' ';
    
    return 0;
}